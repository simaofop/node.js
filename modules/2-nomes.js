// Segredo
const segredo = 'Este é um segredo!';

// Nomes
const nome1 = 'João';
const nome2 = 'Maria';

module.exports = {
  nome1,
  nome2
};

// Secreto não é exportado, então não estará disponível para outros ficheiros que importarem este módulo.